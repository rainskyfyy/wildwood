{
  "name": "wildwood-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright E2E for Wildwood web export (M1.3)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "mock:serve": "node tests/e2e/mock-godot-export/serve.js",
    "mock:install": "node tests/e2e/mock-godot-export/install-deps.js"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
